import HeroPage from "@/features/home/hero-page";

export default function Home() {
  return (
    <main className="flex flex-col items-center justify-center w-full min-h-screen">
      <HeroPage />
    </main>
  );
}
